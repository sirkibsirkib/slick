amy {
    amy wins.
}

bob {
    bob wins if Rule has_consequent (amy wins).
}

amy {
    amy wins
        if Rule has_consequent ((var Var) has_consequent (Winner wins))
        and diff {Winner bob}.
}

bob {
    bob wins
        if Rule has_consequent ((var Var2) has_consequent ((var Var) has_consequent (Winner wins)))
        and diff {Winner bob}.
}