main {
	main says (X says (trusted X))
		if X says (trusted Anyone).

	<X says (trusted Z)
		if X says (trusted Y)
		and Y says (trusted Z).>
}

amy {
	# trusted bob.
	amy says (trusted main).
	amy says X if A says X
		and amy says (trusted X).
}

bob {
	#dogs are neat.
}